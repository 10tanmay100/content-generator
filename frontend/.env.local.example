# Base URL of the FastAPI backend (Azure Container App or local uvicorn)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Token sent as Authorization: Bearer <token> to the backend
BACKEND_API_TOKEN=change-me-local-dev-token
